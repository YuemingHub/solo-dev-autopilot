# 2026-08-03 — Solo Dev Autopilot v2 开发

## 状态

- 仓库路径：`F:\project\solo-dev-autopilot`（原 ZWNJ 路径 `?project?` 已由用户手动改名修复，文件工具全部恢复可用）
- 远程：https://github.com/YuemingHub/solo-dev-autopilot（origin/main，本地领先 1 个 commit 未推送）
- 蓝图：`docs/BLUEPRINT-v2.md`，用户已确认 D1-D6 全部按建议执行

## 已完成

- **Phase 1**（commit `d34e445`）：9 个 skill 迁移到 `.claude/skills/<name>/SKILL.md` 官方格式、三级权限 settings.json、硬伤修复
- **Phase 2**（commit `0b54926`）：
  - onboarding skill（引导装 superpowers + 首次对话四步走）
  - deploy-check → deploy-gate（+人工确认红线不可跳过 +CI 检查 +密钥扫描 +回滚指南 +中国部署场景）
  - context-map 记忆裁剪策略（100 文件项目 ≤2000 行）
  - setup.sh/.ps1 改为文件夹式 SKILL.md 安装 + superpowers 自动检测引导
  - 插件市场注册 `.claude/plugin/marketplace.json` + `.claude-plugin/`（schema 对齐 obra 官方）
  - 删除 git-workflow（superpowers 已覆盖）
- **Phase 3（生产化）**（commit `3b5d3a4`，已推送 origin/main）：
  - `configs/permissions.json` 三级危险度权限模型（safe 自动放行 / ask 确认 / danger 拒绝+确认，唯一事实源）
  - `configs/modes/toy.json` / `team.json` / `production.json` 三档配置（权限+hook严格度+记忆策略+Claude 权限片段）
  - 4 个新 skill：test-runner（测试闭环+覆盖率≥80%）/ ci-helper（CI 配置+排障）/ observability（JSON日志+Sentry+健康检查+告警）/ production-preflight（六维预检）
  - `.github/workflows/ci.yml` CI 模板（lint→test→build→audit→secret scan + 覆盖率门 80%）
  - pre-commit-hook：严格度随 `.autopilot-mode` 调整（toy=仅警告 / team&production=阻止）；shebang 改 `#!/bin/sh`（Windows 兼容）；any 检查跳过注释行
  - setup.sh/.ps1：新增模式选择 + pre-commit/pre-push 安装带备份；新增 post-session.ps1（Windows 版）
  - docs/production-checklist.md + docs/autopilot-boundaries.md
  - 修复 install-git-hooks.ps1 无 BOM 导致 PS 5.1 解析失败（补 UTF-8 BOM）
- **Phase 4（文档与评分）**（进行中，待提交）：
  - README 架构图（四层：项目 → AI 工具 → 配置层 → 上游底座）
  - docs/tool-setup.md 各工具安装引导（Claude Code 全适配 + 4 工具社区适配）
  - docs/scoring.md 评分维度表（8 维加权 + 逐项证据链，总分 9.0 计算路径）
  - getting-started.md 补三步开始总览 + 修 v1 残留（平铺 skills/、runAs frontmatter、软链路径）
  - BOOTSTRAP-PROMPT.md 修 skills/ → .claude/skills/ 引用
  - CHANGELOG v2.3 + 蓝图

## 三仓库联动启动（下午）

- 用户确认：solo-dev-autopilot（方法论）↔ Ming-os-production（真实项目）↔ Ming-os-understanding（地图）三仓联动，9 步闭环循环验证
- 决策：production 档、一个 issue 一轮、地图用 OpenSquilla 自动生成（不等 Trae）、agent 自主执行到提交/PR、发布类操作用户拍板
- **Step 0 完成**：production 本地目录（原本无 .git）init + 关联远程 + 本地快照 commit `1500317`（父 = origin/production `55802b8`），工作区干净
  - 网络坑：GitHub SSH 大包传输卡死/HTTPS early EOF（国内网络），浅拉取 + 重试循环成功；期间清过孤儿 git 进程、shallow.lock、tmp_pack
  - partial clone（blob:none）残留坑：commit 触发 geometric repack 读缺失 blob 失败 → `gc.auto=0` 解决；blob 用本地文件 `git add -A` 重建
- 关键发现：
  1. 远程 .gitignore 是 UTF-16 LE（103 行旧版）→ 忽略规则失效 → 63 个运行时文件（chats.json/agent.json/jobs.json/reports/flywheel/* 等）被误提交进远程 production；本地 .gitignore 是 UTF-8 117 行新版（含 chats.json 等规则）领先远程
  2. UNDERSTANDING_INDEX.md 不在远程树（理解仓库 README 声称主项目有该索引）
  3. production 自带 17 skills + 2 CI + AGENTS.md 自主契约，autopilot 应以流程整合为主
- 候选首个试点：修 .gitignore 编码 + 解除 63 个误跟踪文件（codex/<task> 分支 + Draft PR）——待用户确认后执行 Phase 4 勾选
- **CI 自检重构**（commit `1b46811` + `f4210c2`，已推送）：
  - 问题：原 ci.yml 是 Node 模板但本仓库无 package.json，push 必红（Actions 里 3 个失败 run 坐实）
  - `scripts/ci-self-check.sh` 仓库自检脚本（JSON 10 个 / SKILL.md 13 个 / shell 7 个），本地可跑 + CI 复用
  - `.github/workflows/ci.yml` 改为自检流水线；Node 模板移到 `templates/ci-node.yml`
  - 坑：本地 git sh.exe 是 bash 5.3，`sh -n` 全过；CI 的 sh 是 dash 解析 bash 语法脚本必挂 → 统一 `bash -n`（`f4210c2`）
  - 引用同步：README/蓝图/ci-helper/tool-setup/scoring/CHANGELOG
  - README 结构树声称 23 个关键文件全部存在（文档与实物一致性 ✅）
- **CI 模板端到端验证**（commit `42aafb8`，已推送）：
  - 用真实 Node 项目（node v22 + vitest 2.1.9，math.ts 3 测试）跑通 ci-node.yml 整条流水线
  - **发现并修复 3 个模板缺陷**：
    1. 覆盖率门用 `--reporter=json`（vitest 不输出 JSON 到 stdout）门静默失效 → 改 `--coverage.reporter=json-summary` + node 提取 pct
    2. `pnpm audit || true` 让高危漏洞不阻止 CI → 去掉豁免
    3. 密钥扫描 `[A-Za-z0-9]{16,}` 漏掉 sk- 带连字符格式 → 改 `[^'"[:space:]]{16,}` + access[_-]?key
  - 新增 `docs/verification.md` 验证记录（可复现证据，支撑评分）
  - 环境坑：Windows corepack pnpm 11 的 ignored-builds 机制拦截 esbuild（CI 用 pnpm 9 无此问题）；corepack use pnpm@9 失败（packages field 错误）→ 用 node_modules/.bin 直接调用绕过
  - **commit 编码坑**：PowerShell 5.1 `git commit -m "中文"` 会把中文参数变成 `?`（控制台编码）→ 用 `git commit -F 文件`（UTF-8 无 BOM 文件）传消息；git log 在 PowerShell 里显示乱码是显示层问题，cmd 重定向验证存储层是 UTF-8 正常（本次 amend + force-with-lease 修复过 1 次）

## 验证

- setup.sh：`sh -n` 通过（git 自带 `F:\myfile\git\usr\bin\sh.exe`，无独立 bash）
- setup.ps1：PS 解析通过；3 个 JSON 文件解析通过
- 环境注：`python` 是 WindowsApps 占位符不可用；PowerShell 5.1 读 .ps1 按 ANSI，写中文脚本需注意
- Phase 3 验证：6 个 JSON `ConvertFrom-Json` 全过；setup.sh/post-session.sh/install-git-hooks.sh `sh -n` 过；3 个 .ps1 解析器过（install-git-hooks.ps1 补 BOM 后过）
- pre-commit-hook 实测：team 模式硬编码密钥阻止提交（exit 1）✅；toy 模式仅警告不阻止（exit 0）✅
- 推送：代理 7890 端口开启后 `git -c http.proxy=http://127.0.0.1:7890 push` 成功
- Phase 4 验证：`.claude/skills/` 实际 13 个 = README 声称 13 个 ✅；docs 残留扫描干净
- CI 自检验证：本机 23 项断言（10 JSON + 13 SKILL frontmatter）全过；结构树 23/23 文件存在
- **CI 真实运行**：自检 CI 最终连续两绿（`f4210c2` success + `acef976` success）；修复前 run 均失败（dash 解析 bash 语法误报，已用 bash -n 解决）；GitHub API 限流 60/h 需等重置
- **CI 模板端到端**：3 tests passed / 覆盖率 100% ≥80% / lint+build 过 / audit 正确标出 critical / secret-scan 6 案例两向全过

## 下一步

- ✅ 全部 Phase 1-4 + CI 自检修复 + CI 模板验证完成并推送（origin/main 最新 `42aafb8`）
- 交付总结（评分 9.0 证据链）已就绪：见 docs/scoring.md + docs/verification.md
- 可选后续：真实 Node 项目用 templates/ci-node.yml 验证覆盖率门、发布 release tag
