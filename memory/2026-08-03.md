# 2026-08-03 — Solo Dev Autopilot v2 开发

## 状态

- 仓库路径：`F:\project\solo-dev-autopilot`（原 ZWNJ 路径 `?project?` 已由用户手动改名修复，文件工具全部恢复可用）
- 远程：https://github.com/YuemingHub/solo-dev-autopilot（origin/main，本地领先 1 个 commit 未推送）
- 蓝图：`docs/BLUEPRINT-v2.md`，用户已确认 D1-D6 全部按建议执行

## 已完成

- **Phase 1**（commit `d34e445`）：9 个 skill 迁移到 `.claude/skills/<name>/SKILL.md` 官方格式、三级权限 settings.json、硬伤修复
- **Phase 2**（commit `0b54926`）：
  - onboarding skill（引导装 superpowers + 首次对话四步走）
  - deploy-check → deploy-gate（+人工确认红线不可跳过 +CI 检查 +密钥扫描 +回滚指南 +中国部署场景）
  - context-map 记忆裁剪策略（100 文件项目 ≤2000 行）
  - setup.sh/.ps1 改为文件夹式 SKILL.md 安装 + superpowers 自动检测引导
  - 插件市场注册 `.claude/plugin/marketplace.json` + `.claude-plugin/`（schema 对齐 obra 官方）
  - 删除 git-workflow（superpowers 已覆盖）
- **Phase 3（生产化）**（进行中，待提交）：
  - `configs/permissions.json` 三级危险度权限模型（safe 自动放行 / ask 确认 / danger 拒绝+确认，唯一事实源）
  - `configs/modes/toy.json` / `team.json` / `production.json` 三档配置（权限+hook严格度+记忆策略+Claude 权限片段）
  - 4 个新 skill：test-runner（测试闭环+覆盖率≥80%）/ ci-helper（CI 配置+排障）/ observability（JSON日志+Sentry+健康检查+告警）/ production-preflight（六维预检）
  - `.github/workflows/ci.yml` CI 模板（lint→test→build→audit→secret scan + 覆盖率门 80%）
  - pre-commit-hook：严格度随 `.autopilot-mode` 调整（toy=仅警告 / team&production=阻止）；shebang 改 `#!/bin/sh`（Windows 兼容）；any 检查跳过注释行
  - setup.sh/.ps1：新增模式选择 + pre-commit/pre-push 安装带备份；新增 post-session.ps1（Windows 版）
  - docs/production-checklist.md + docs/autopilot-boundaries.md
  - 修复 install-git-hooks.ps1 无 BOM 导致 PS 5.1 解析失败（补 UTF-8 BOM）

## 验证

- setup.sh：`sh -n` 通过（git 自带 `F:\myfile\git\usr\bin\sh.exe`，无独立 bash）
- setup.ps1：PS 解析通过；3 个 JSON 文件解析通过
- 环境注：`python` 是 WindowsApps 占位符不可用；PowerShell 5.1 读 .ps1 按 ANSI，写中文脚本需注意
- Phase 3 验证：6 个 JSON `ConvertFrom-Json` 全过；setup.sh/post-session.sh/install-git-hooks.sh `sh -n` 过；3 个 .ps1 解析器过（install-git-hooks.ps1 补 BOM 后过）
- pre-commit-hook 实测：team 模式硬编码密钥阻止提交（exit 1）✅；toy 模式仅警告不阻止（exit 0）✅

## 下一步

- 提交 Phase 3 并推送 origin/main（本地当前领先 3 个 commit）
- **Phase 4（文档与评分）**：README 定位声明（已部分更新）、评分维度表、三步开始流程完善、competitive-analysis.md
