# 2026-08-04 — 三仓库联动第一轮试点（Mingos 运行态产物解除跟踪）

## 本轮执行（9 步闭环第 1 轮）

- **试点任务**：`.gitignore` 已含 `reports/flywheel/` 规则但历史已跟踪文件不自动解除，收尾解除 53 个运行态 session JSON 的跟踪。
- **执行**：从 production `35d56d5` 建 `codex/fix-flywheel-untrack-v2` → `git rm --cached` 精确 53 文件 → 验证 diff 恰好 53 个 D → `test:release-install-integrity` + `test:current-state-privacy` 通过 → 提交 → 推送 → Draft PR #110。
- **记录**：Ming-os-understanding PROJECT_STATE.md 决策表 + 开放 Draft 清单更新，已推送。

## 发现的方法缺口（autopilot 优化输入）

1. **旧分支混删活源码**：`codex/fix-gitignore-tracking`（PR #109）把 `src/tests/test-llm-fallback.js`（被 `test:llm-fallback` 引用）与 RUNBOOK 文档一起 `git rm`，范围污染。教训：解除跟踪前必须 `rg` 确认文件被引用情况，且 diff 范围只允许目标文件。
2. **git rm --cached 的「规则已加但历史跟踪不解除」是高频坑**：适合沉淀为 context-map/仓库卫生检查项——改动 .gitignore 后应跑 `git ls-files | check-ignore` 核对存量。
3. **提交中文走 UTF-8 无 BOM 消息文件**（PowerShell 5.1 直传参数会变 `?`），本仓库 memory/2026-08-03.md 已记录，本轮再次验证有效。
4. **推 GitHub 走 127.0.0.1:7890 代理**，有效（Ming-os 与 Ming-os-understanding 均成功）。

## 下一步候选

- PR #110 是否合入由月明拍板；建议关闭 #109。
- Mingos 下一轮试点任务待选：UI 移动端审计未完成项（退出登录/旧路由/数据权利交互）或补齐 flywheel 之外的运行产物（knowledge/fki/compiled 是否该入库需按知识治理决策，不要沿用 #109 一并删除）。
