# 🔄 Auto-Evolve 变更记录

> 本文件由 `scripts/auto-evolve.sh` 生成候选变更，人工审核后合并到这里。
> 每周自动扫描（`.github/workflows/auto-evolve.yml`）会把候选写入 `EVOLVE_CHANGELOG.tmp`，确认后合并。

## 2026-08-03

- 初始化本文件，补齐 auto-evolve 工作流引用的记录载体（v2.3.3）。
